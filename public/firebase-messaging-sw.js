// Auto-generated by scripts/build-sw.js — do not edit manually.
importScripts("https://www.gstatic.com/firebasejs/10.13.0/firebase-app-compat.js");
importScripts("https://www.gstatic.com/firebasejs/10.13.0/firebase-messaging-compat.js");

firebase.initializeApp({
  apiKey: "AIzaSyCdkEYlyjCgzcgQn6yFVHPI_igy0e0kJ0M",
  authDomain: "idg-crm.firebaseapp.com",
  projectId: "idg-crm",
  storageBucket: "idg-crm.firebasestorage.app",
  messagingSenderId: "831560014827",
  appId: "1:831560014827:web:7d4b0d2c6e5a51c6b6d7ae",
});

const messaging = firebase.messaging();

messaging.onBackgroundMessage(({ notification, data }) => {
  if (!notification) return;
  self.registration.showNotification(notification.title || "Notificación CRM", {
    body: notification.body || "",
    icon: "/logoIDG.jpg",
    badge: "/logoIDG.jpg",
    data: data || {},
  });
});

self.addEventListener("notificationclick", (event) => {
  event.notification.close();
  const link = event.notification.data?.link || "/";
  event.waitUntil(
    clients
      .matchAll({ type: "window", includeUncontrolled: true })
      .then((windowClients) => {
        for (const client of windowClients) {
          if (client.url.includes(self.location.origin) && "focus" in client) {
            client.navigate(link);
            return client.focus();
          }
        }
        return clients.openWindow(link);
      })
  );
});
